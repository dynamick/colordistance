{
  "name": "colordistance",
  "version": "1.0.0",
  "description": "Change color temperature of a led strip based on the distance measured by a sonar.",
  "main": "index.js",
  "scripts": {
    "build": "babel src -d lib",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "mmm-usonic": "^2.5.3",
    "pi-blaster.js": "^0.1.1",
    "pigpio": "^0.4.0"
  },
  "devDependencies": {
    "babel-cli": "^6.22.2",
    "babel-preset-es2015": "^6.22.0"
  }
}
